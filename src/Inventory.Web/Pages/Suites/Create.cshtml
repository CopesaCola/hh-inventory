@page
@model Inventory.Web.Pages.Suites.CreateModel
@{
    ViewData["Title"] = "Add Suite";
}

<h1>Add Suite</h1>

<form method="post" class="card">
    <div asp-validation-summary="All" class="error"></div>
    <label>Name</label>
    <input type="text" asp-for="Input.Name" placeholder="Pharmacy, Lab 2, Conference Room A..." />
    <span asp-validation-for="Input.Name" class="error"></span>

    <label>Site</label>
    <select asp-for="Input.SiteId">
        <option value="">(select a site)</option>
        @foreach (var s in Model.Sites)
        {
            <option value="@s.Id">@s.Name</option>
        }
    </select>
    <span asp-validation-for="Input.SiteId" class="error"></span>

    <div class="actions">
        <button type="submit">Save</button>
        <a class="button secondary" asp-page="Index">Cancel</a>
    </div>
</form>
